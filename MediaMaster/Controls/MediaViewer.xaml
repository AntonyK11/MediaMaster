<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="MediaMaster.Controls.MediaViewer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MediaMaster.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{x:Bind Background, Mode=OneWay}" CornerRadius="{x:Bind CornerRadius, Mode=OneWay}">
        <Grid.RowDefinitions>
            <RowDefinition Height="300" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border
            Grid.Row="0"
            HorizontalAlignment="Stretch"
            Background="{ThemeResource LayerFillColorDefaultBrush}">
            <local:MediaIcon
                x:Name="MediaIcon"
                MaxHeight="300"
                ImageMode="ThumbnailOnly" />
        </Border>

        <ScrollView Grid.Row="1">
            <StackPanel
                x:Name="StackPanel"
                Margin="16"
                Spacing="16">
                <Grid
                    MinHeight="24"
                    HorizontalAlignment="Center"
                    ColumnSpacing="12">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="24" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Border
                        Grid.Column="0"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        CornerRadius="4">
                        <Image Name="MediaExtensionIcon" />
                    </Border>

                    <local:EditableTextBlock
                        x:Name="NameTextBox"
                        Grid.Column="1"
                        PlaceholderText="Media name" />
                </Grid>
            </StackPanel>
        </ScrollView>
    </Grid>
</UserControl>
